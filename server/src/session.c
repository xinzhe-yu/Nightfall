#include "session.h"
#include "types.h"

static session_t *sess_list;

session_t *session_create(uint64_t id, const char *hostname) {
}

session_t *session_find(uint64_t id) {
}

void session_touch(session_t *sess) {
}

void session_destroy(uint64_t *sess) {
}